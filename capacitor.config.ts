import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.organizadorfcil',
  appName: 'organizadorfácil',
  webDir: 'dist'
};

export default config;
